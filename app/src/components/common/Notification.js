import React from 'react';
import styled from 'styled-components';

const NotiContainer = styled.div``;

const Notification = () => {
    return <NotiContainer></NotiContainer>;
};

export default Notification;
